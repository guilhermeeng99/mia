# MIA — Roadmap

> **Status**: Phase 1 (Core Dictation MVP) **code-complete** + **core loop validated on Windows** 🎉. The end-to-end loop runs on a real machine: global PTT → cpal capture → **server-side Silero-VAD-gated** warm whisper-server (pt-BR) → deterministic cleanup → SendInput injection, with a **user-selectable recording indicator** (`hud.indicator`: floating HUD overlay, a tray-icon badge, or both — chosen in the Ditado view; default both). Now also done: **close-to-tray** and **focused-target + elevated-window (UIPI) detection** on the injection path (`win32.rs`), on top of the earlier language selection, dictionary bias prompt, hotkey recorder + watchdog, onboarding gate, snippet toggle, and launch-at-login. A dictation session ends only on an explicit user action (hotkey release / 2nd toggle press) — never on silence (the earlier energy-gated auto-endpoint was removed; the cargo-tested `EndpointDetector` stays as the primitive for a backlog in-process Silero endpoint). **Phase 3 (Personalization) is code-complete**: custom dictionary, snippets, and **per-app writing styles/context** (keyed to the focused EXE) all land. Phase 4: **signed auto-update** + **NVIDIA CUDA** (validated on an RTX 4050) done; the **mic-test live meter** and **mic permission-denied deep-link** are now wired. **Phase 2 (AI) is descoped** by product decision — MIA stays a faithful dictation tool. The **release pipeline mirrors Toolzy** — `release.yml` auto-bumps, tags, and publishes a signed, live installer + `latest.json` on every push to `main`; both signing secrets are set and **signed releases are live** (see GitHub Releases for the latest). A standalone marketing landing page lives in `site/` (Vite + pnpm), deployed by `deploy-site.yml`. **App icons/branding now ship** (the supplied logo was rasterized into the full icon set). The only genuinely remaining work is **hardware-gated**: on-device-validate the new FFI/endpoint paths and the held-back major dep bumps. See "Next / open".
> **Last updated**: 2026-06-04
> **Environment**: desktop (Windows, native)
> Single source of truth for what is **done**, **in progress**, and **planned**. Update in the same change that shifts scope (see [`/CLAUDE.md`](../CLAUDE.md) → Post-Change Checklist).

MIA is a free, open-source, **privacy-first, fully local** voice-to-text **dictation** app for **Windows** — press a global push-to-talk hotkey, speak, and MIA types polished text at the cursor in whatever app is focused. Everything runs on the user's machine: no cloud, no account, no server, the voice never leaves the device. It is the offline answer to [Wispr Flow](https://wisprflow.ai) (cloud-based). The engine is **Rust** ([Tauri 2](specs/architecture.md#adr-002-tauri-2--svelte-5--vite--tailwind-v4-bun-tooling)); the UI is a thin [Svelte 5](specs/design-system.md) webview for settings/onboarding/HUD only. Brazilian Portuguese (pt-BR) and English are first-class; Whisper covers ~99 languages. See the full decision log in [docs/specs/architecture.md](specs/architecture.md).

## Legend

✅ Done · 🚧 In progress · ⬜ Planned · 💡 Backlog

---

## Status at a glance

| Phase | Theme | Status |
|---|---|---|
| 0 | Docs & Design — this documentation set + design system | ✅ Done |
| 1 | Core Dictation MVP — PTT → capture → VAD → warm STT → cleanup → inject; tray + selectable indicator (overlay/tray/both); pt-BR + English | ✅ Code-complete (validated on Windows) |
| 2 | AI Magic — optional local LLM Command Mode + Polish | ❌ Descoped (product decision) |
| 3 | Personalization — custom dictionary, snippets, per-app writing styles/context | ✅ Code-complete |
| 4 | Polish & Distribution — onboarding, settings/"Hub" dashboard + stats, signed auto-update, NVIDIA CUDA, release pipeline | ✅ Done — signed releases live |
| 5 | Backlog — streaming partials, wake word, Whisper Mode, macOS/Linux, file-transcription mode | 💡 Backlog |

---

## Detail

### Phase 0 — Docs & Design ✅

Define the product, architecture, and design system before any code is written. This phase is **complete**; Phase 1 implementation is now underway.

- ✅ Documentation set: [`/README.md`](../README.md), [`/CLAUDE.md`](../CLAUDE.md), this roadmap, and all feature specs under [docs/specs/](specs/) following [docs/specs/_template.md](specs/_template.md). Historical planning docs were removed from the active set once their decisions were folded into the roadmap/specs.
- ✅ [Architecture](specs/architecture.md) — the canonical ADRs (ADR-001 … ADR-011) and the "Rust is the engine, Svelte is a thin webview" principle.
- ✅ [Design system](specs/design-system.md) — the **"Blush Playground" (Lpalo)** identity: blush-pink canvas, Alfa Slab One + Manrope, 2px charcoal outlines (no shadows/gradients), pill controls. The **Settings/Hub** is a sidebar + per-view shell; the **floating mic HUD** is a white outlined pill in the same language. Shared UI components in `app/src/lib/components/ui/`. _(Redesigned 2026-05-30 from the original "Calm Focus" blue identity.)_

### Phase 1 — Core Dictation MVP ✅ (code-complete, validated on Windows)

The end-to-end live loop: global PTT hotkey → cpal capture → Silero VAD (silence gating) → **warm** whisper.cpp → deterministic cleanup → SendInput injection at the cursor; system tray + selectable recording indicator (overlay/tray/both); on-demand model download gate; pt-BR + English. Orchestration spec: [dictation.md](specs/dictation.md). All IPC uses `Result<T, String>` ([ADR-006](specs/architecture.md#adr-006-result-error-model)); pure helpers carry `#[cfg(test)]` cargo tests.

- ✅ **Scaffold** the Tauri 2 + Svelte 5 (runes) + Vite + TypeScript (strict) + Tailwind v4 project with **Bun** tooling (pnpm fallback) and WebView2. Engine modules live under `app/src-tauri/src/*.rs`; the `#[tauri::command]` registry + warm-STT managed state are wired in `lib.rs`; typed `invoke()` wrappers begun in `app/src/lib/*.ts` (`stt.ts`, `inject.ts`). → [architecture.md](specs/architecture.md) · [ADR-002](specs/architecture.md#adr-002-tauri-2--svelte-5--vite--tailwind-v4-bun-tooling)
- ✅ **Audio capture** — `cpal` mic input at 16 kHz mono PCM, device selection, level metering. Pure DSP core done + cargo-tested (stereo→mono downmix, linear 48→16 kHz resample, `f32`→`s16` quantize, RMS/peak, `FrameChunker`, device-name normalize); `list_input_devices` command live. `app/src-tauri/src/audio.rs`. cpal capture thread (`!Send`-safe) + mono accumulation + `Level` meter (now streamed live to the HUD) + `begin_capture`/`end_capture` (in-process) + `test_microphone` command + Hub mic-test button — compile/build-verified. VAD-gated trimming is done server-side (whisper-server `--vad`). A capture session **never auto-ends on silence** — it ends only on an explicit user action (hotkey release / 2nd toggle press), so a pause mid-utterance keeps recording (the earlier energy-gated auto-endpoint was removed; in-process Silero ONNX stays a backlog optimization). → [audio-capture.md](specs/audio-capture.md) · [ADR-001](specs/architecture.md#adr-001-native-on-device-privacy-first)
- ✅ **Silero VAD endpointing** — voice-activity gating + utterance endpoint detection feeding the STT. Endpoint state machine done + cargo-tested (debounce `MIN_SPEECH_MS`, hangover `MIN_SILENCE_MS`, all-silence → nothing, re-arm; Rules 4/5/8), with the fixed Silero constants. `app/src-tauri/src/vad.rs`. Recognition is now **VAD-gated server-side**: the warm whisper-server runs with `--vad --vad-model <silero>` (the model downloaded alongside Whisper; boot-verified), so only detected speech reaches the decoder — the warm equivalent of the planned per-frame gate. The `vad.rs` per-frame `EndpointDetector` is retained, cargo-tested, as the primitive for a **backlog in-process Silero endpoint** path; it is **not** wired to the live path (a session ends only on an explicit user action, never on silence). → [audio-capture.md](specs/audio-capture.md), [speech-to-text.md](specs/speech-to-text.md) · [ADR-007](specs/architecture.md#adr-007-on-demand-models--anti-hallucination)
- ✅ **Warm whisper-server STT** — resident/warm model loaded once via a **whisper-server** sidecar (ADR-004 revised MVP default, cmake-free; **whisper-rs in-process** is the later optimization behind the same seam), NOT a cold `whisper-cli` spawn per utterance. On-demand model download from Hugging Face to app-data with a download gate, `.part` rename, streamed progress via a Tauri `Channel` — adapted from Toolzy's file transcription engine for MIA's live path: shared registry/URL/download/GPU acquisition patterns, but mic capture + warm lifecycle instead of ffmpeg + cold per-file CLI spawn. Per-request anti-hallucination fixed: greedy (temperature 0, no fallback ladder) + independent per-utterance `/inference`; Silero VAD model fetched alongside the model. Warm lifecycle (spawn/wait/drop), in-memory `transcribe_chunk`, and the `warm_status` command are complete + tested. `app/src-tauri/src/stt.rs`. The engine binary now ships with the installer: `app/scripts/fetch-binaries.mjs` fetches `whisper-server.exe` + sibling ggml/whisper DLLs into `app/src-tauri/binaries/`, and `tauri.conf.json` bundles them via `bundle.resources` (the engine is spawned via `std::process::Command` on a resolved resource path, not a Tauri sidecar). `transcribe_chunk` is now wired into the live dictation orchestrator (`dictation.rs` — capture → warm STT → cleanup → inject, with cancellation); see the *Wire the end-to-end pipeline* item below. Remaining (runtime): live Silero VAD-gated trimming on the capture path. → [speech-to-text.md](specs/speech-to-text.md) · [ADR-003](specs/architecture.md#adr-003-whisper-stt), [ADR-004](specs/architecture.md#adr-004-warm-resident-stt), [ADR-007](specs/architecture.md#adr-007-on-demand-models--anti-hallucination)
- ✅ **Deterministic cleanup** — a pure Rust module: filler-word stoplist (um/uh/é/tipo/né…), spoken-punctuation substitution ("nova linha", "ponto", "vírgula"), stutter/repeat collapse, whitespace normalization, sentence-case/capitalization fixer, light number spacing, optional trailing period. Always-on, fidelity-safe; per-language (pt-BR/En/Other) rule sets; cargo-tested rules. `app/src-tauri/src/cleanup.rs`. → [text-cleanup.md](specs/text-cleanup.md) · [ADR-008](specs/architecture.md#adr-008-hybrid-text-intelligence)
- ✅ **Windows text injection** — `enigo` SendInput Unicode keystrokes (default) + `arboard` clipboard + simulated Ctrl+V fallback for long text (save & restore the user's clipboard), behind the `TextInjector` trait with a runtime-selected backend; grapheme-safe chunking + `pick_backend` Auto decision, all cargo-tested and wired through the in-process orchestrator path. `app/src-tauri/src/inject.rs`. **Focused-target + elevated-window (UIPI) detection (Rules 6–7) now wired** in the orchestrator via `win32.rs` (`GetForegroundWindow` + token-elevation probe): an elevated/UAC target returns a run-as-administrator error instead of silently dropping keystrokes, and no detectable foreground window falls back to the clipboard. Full editable-target detection (UIAutomation) stays out of scope; on-device UAC validation is owner-gated. → [text-injection.md](specs/text-injection.md) · [ADR-005](specs/architecture.md#adr-005-system-wide-text-injection)
- ✅ **Global PTT hotkey** — `tauri-plugin-global-shortcut`, push-to-talk that works while unfocused; press-and-hold + toggle modes. Pure core done + cargo-tested: accelerator parser/canonicalizer (round-trips, exact error messages), bare-key + reserved-chord guards, and the debounce + activation-mode `reduce()` reducer (Rules 3/4/9/10). `app/src-tauri/src/hotkey.rs`. Runtime via `tauri-plugin-global-shortcut`: registration + handler (runs `reduce`, emits `dictation://intent`) + startup registration + `register/unregister/update/get_hotkey` commands + `key_to_code`/`to_shortcut` (cargo-tested) + `ptt.ts` frontend wiring — compile/build-verified, validated on Windows. Esc-cancel (a transient `Escape` binding registered only while a session is active → Cancel) and the missing-release watchdog (push-to-hold force-Stop after `MAX_HOLD_MS`, disarmed by a session generation) are now wired in the runtime; the plugin handler routes Esc vs the PTT chord. The Hub now has a chord **recorder** (capture a modifier+key combo) + activation-mode picker wired to `update_hotkey`, which re-registers and **rejects a conflicting chord** (the conflict-probe), keeping the old binding on failure. A **resume/unlock + idle self-heal** watcher (`power_resume.rs`, [hotkeys.md](specs/hotkeys.md) Rule 15) silently re-claims the global PTT registration after sleep-resume or session-unlock (and on an idle tick), with a tray **"Reativar atalho"** manual fallback. The hotkey stage is feature-complete (pending only broad on-device chord testing). → [hotkeys.md](specs/hotkeys.md) · [ADR-005](specs/architecture.md#adr-005-system-wide-text-injection)
- ✅ **System tray + floating mic HUD** — `tray-icon` crate for the tray; a white Blush (outlined pill) always-on-top mic HUD with the listening → transcribing → inserting → error state machine and a live waveform/level meter. System tray (Tauri 2 built-in `tray-icon`, `tray.rs`: Open / Reativar atalho / Quit) wired in setup — compile/build-verified, validated on Windows. HUD `MicHud.svelte` (state machine + RMS waveform) now lives in a **dedicated transparent, always-on-top, click-through HUD window** (`hud.rs` docks it bottom-center + makes it click-through; `tauri.conf` window `focus:false` so it never steals focus from the injection target; `HudWindow.svelte` mounts it on `?win=hud`), driven directly by the engine's `hud://state` events and a live `hud://level` RMS waveform (the capture thread streams it at ~16 Hz). **Close-to-tray is wired**: closing the Hub hides it to the tray (`lib.rs` `on_window_event` → `prevent_close` + `hide`); only the tray "Sair" exits. → [tray-and-hud.md](specs/tray-and-hud.md), [design-system.md](specs/design-system.md)
- ✅ **Wire the end-to-end pipeline** — orchestrate hotkey → capture → VAD → warm STT → cleanup → inject, with HUD state transitions and cancel. **Validated on Windows** (pt-BR utterance → VAD-trimmed → transcribed → injected). A re-entrant global-shortcut deadlock (registering the transient Esc binding from inside the plugin handler) was found and fixed by emitting the intent first and deferring shortcut (un)registration off the handler thread. Pure orchestrator core in `dictation.rs` cargo-tested: `next_phase` HUD state machine (illegal-signal no-op, cancel-from-any), `interpret_down`, `classify_cancel`, `build_result`. The `start/stop/cancel_dictation` commands wire the real pipeline end-to-end (cpal capture → warm STT → cleanup → dictionary → snippets → inject, emitting HUD events + recording stats) + `dictation.ts` — compile/build-verified. The global Ctrl+Space PTT now drives it end-to-end (hotkey → `dictation://intent` → `ptt.ts` → start/stop) and the floating HUD window reflects each phase with a live RMS waveform (`hud://state` + `hud://level`, emitted by the orchestrator/capture thread). A session ends only on an explicit user action (release / 2nd toggle press) — never on silence. The orchestrator also resolves the focused app at start for the per-app style + UIPI checks. → [dictation.md](specs/dictation.md) · [ADR-004](specs/architecture.md#adr-004-warm-resident-stt)
- ✅ **pt-BR + English** — first-class language selection: a Hub picker (Automático / Português (pt-BR) / English) persisted in `settings.general.default_language`; the orchestrator reads it per utterance and forwards `language=` to `/inference` (auto-detect when unset), so the choice is remembered with no warm-engine restart. → [speech-to-text.md](specs/speech-to-text.md) · [ADR-003](specs/architecture.md#adr-003-whisper-stt)

### Phase 2 — AI Magic ❌ (descoped)

Dropped by product decision (2026-05-29): MIA stays a faithful, deterministic dictation tool — no local-LLM Command Mode / Polish. A warm `llama-server` runtime was built and then reverted; the dormant pure helpers (`ai_commands.rs`) have since been removed from the engine — the design record is preserved in [docs/specs/ai-commands.md](specs/ai-commands.md) + git history. See "Out of scope (deliberate)" below.

### Phase 3 — Personalization ✅ (code-complete)

- ✅ **Custom dictionary** — personal vocabulary / word replacement (names, jargon, acronyms). Pure mechanism-(a) core in `dictionary.rs` cargo-tested (`apply_dictionary` exact/case/whole-word/multi-word/fuzzy/longest-match/idempotent, `match_case`, `fuzzy_match`, `build_bias_prompt`, `validate_entry`; Rules 1-13). CRUD commands + atomic `dictionary.json` persistence + managed state + duplicate rejection + `dictionary.ts` wrapper done (build-verified). Hub dictionary section (add/remove/enable) wired + build-verified. Bias-prompt wiring into the warm-Whisper call is done (fed as the `/inference` initial prompt). → [custom-dictionary.md](specs/custom-dictionary.md)
- ✅ **Snippets** — voice-triggered text expansion. Pure core in `snippets.rs` cargo-tested (`expand_snippets` whole-phrase/word-boundary/longest-first/no-recursion, `compile_snippets`, `normalize_trigger` case+accent fold, `apply_case`, `validate_snippet`; Rules 1-11). CRUD commands + atomic `snippets.json` persistence + managed state + duplicate-trigger rejection + `preview_expansion` + `snippets.ts` wrapper done (build-verified). Hub snippets section (add/remove + live preview) wired + build-verified. Master enable toggle done: `settings.general.snippets_enabled` gates expansion in the orchestrator, with a toggle in the snippets section. → [snippets.md](specs/snippets.md)
- ✅ **Per-app writing styles / context** — overrides keyed to the focused application's executable (resolved by `win32::foreground_process_name` at session start): pin a language, force a clipboard-vs-SendInput backend, a trailing period, or spoken-punctuation on/off. Pure matching/merge core in `app_styles.rs` cargo-tested (`match_style` longest-substring, `merge_cleanup`, `resolve_language`/`resolve_inject_mode`, `sanitize`); persisted as the `perApp` settings group; applied in the orchestrator (language before STT, cleanup/backend at inject); Hub "Estilos por app" section wired. Off by default. On-device validation owner-gated. → [per-app-context.md](specs/per-app-context.md)

### Phase 4 — Polish & Distribution ✅

- ✅ **Onboarding** flow — first-run: hotkey, mic, model download. `Onboarding.svelte` wizard (welcome → hotkey → mic test → model picker listing all four models, `small` flagged Recomendado, no skip) + `hotkey.ts` wrapper. `App.svelte` shows it only when onboarding hasn't been completed and the active selected model is not installed; downloading or choosing a model persists `settings.model.model`, and "Concluir" persists `settings.general.onboarding_completed=true` so MIA then boots straight to the Hub/tray (Rule 1/14). **Mic permission-denied deep-link** now wired: a denied capture is tagged (`classify_mic_error` → `mic-permission-denied:` sentinel) and the mic-test step (and the Hub) surface an "Abrir configurações de microfone" button that launches `ms-settings:privacy-microphone`. → [onboarding.md](specs/onboarding.md)
- ✅ **Settings / "The Hub"** dashboard + usage stats. Shared design-system primitives (`Button`, `Card`, `Field`, `Toggle`, `Pill`) + a first Hub surface (persisted mic device picker, persisted active Whisper model, on-demand download with live progress, cleanup toggles, warm-engine + GPU status) wired to the typed `invoke()` wrappers; build-verified (svelte-check + vite). Backed by a real `settings.rs` persistence layer (single `settings.json`: pure `apply_patch`/`validate`/`migrate`/`parse` core cargo-tested, failure-safe load, atomic save, `get/update/reset_settings` commands + managed state loaded at startup; `settings.ts` wrapper). Local-only usage stats in `stats.rs` (pure WPM + day-streak arithmetic + word count, cargo-tested; `get_stats`/`reset_stats` commands + managed state). `update_settings` side effects now applied: PTT hotkey re-register on change with rollback, warm-engine invalidation on a model change (lazy re-warm), and **launch-at-login** (via `tauri-plugin-autostart` — synced at startup + on toggle, with a Hub toggle). Per-dictation stat recording is gated by `collectStats`. The **mic test now streams a live level meter from the selected device** (the `test_microphone` command takes a `Channel<CaptureEvent>` plus device id; the Hub + onboarding render a live bar), and the signed updater is wired (below). → [settings.md](specs/settings.md), [design-system.md](specs/design-system.md)
- ✅ **Dictation history** — the Hub has a local history of final dictated text (`history.rs` + `history.json`), recorded before injection so a lost or rejected insertion can still be copied. Entries are local-only, newest-first, capped, copyable, removable, and clearable. → [dictation-history.md](specs/dictation-history.md)
- ✅ **Signed in-app auto-update** — `tauri-plugin-updater` + `tauri-plugin-process` wired (mirrors Toolzy): `plugins.updater` in `tauri.conf.json` points at the repo's `latest.json` with a minisign **pubkey** baked in + `bundle.createUpdaterArtifacts`; `update.ts` (`check`/`downloadAndInstall`/`relaunch`) is auto-invoked on launch (like Toolzy) and surfaces an **"Atualizar para vX" button in the Hub header** only when a newer signed release exists — no manual "check" button; capabilities grant `updater:default` + `process:allow-restart`. The minisign keypair was generated, the pubkey is baked into config, and **both signing secrets are now set in the repo** (`TAURI_SIGNING_PRIVATE_KEY` + the empty `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`). The signed updater is **live**: releases auto-cut on every push to `main`. → [ADR-009](specs/architecture.md#adr-009-distribution--auto-update)
- ✅ **NVIDIA CUDA engine** — optional on-demand GPU engine (`nvcuda.dll` detect → download the cuBLAS whisper build → extract into `engine-cuda/`; **≈7–10×** faster), using the same acquisition pattern as the STT model downloads. Detection verified on an RTX 4050; a Hub "Baixar engine GPU" button drives `download_gpu_engine` (which now unloads the warm CPU server so the next dictation re-warms on the GPU build); `server_exe` already prefers the GPU build. → [speech-to-text.md](specs/speech-to-text.md) · [ADR-007](specs/architecture.md#adr-007-on-demand-models--anti-hallucination)
- ✅ **GitHub release pipeline** — a single `.github/workflows/release.yml` **mirrors Toolzy's auto-release flow**: on every push to `main`, the `ci` job (Biome 1.9.4 + `bun run build` + `cargo clippy -D warnings` + `cargo test`, binaries fetched so `tauri-build` validates) gates a `release` job that auto-bumps the patch version (tauri.conf.json + Cargo.toml + package.json in lockstep), commits `chore(release): vX [skip ci]`, tags `vX`, and publishes a **signed, live** Windows installer via `tauri-action` (+ `createUpdaterArtifacts` emitting `latest.json`). PRs run `ci` only; the bot's bump commit is skipped (`[skip ci]` + an `if:` guard). Both signing secrets are set in the repo, so **signed releases auto-cut on every push to `main`**; the current version is `v0.1.8` — no manual tag. → [ADR-009](specs/architecture.md#adr-009-distribution--auto-update)

---

## Next / open

Phases 0, 1, and 3 are code-complete and the core loop is validated on Windows; the items below are the genuine remainder. The first three are **code-complete, open only on a product/owner decision**:

- ✅ **No silence timeout** — a dictation session ends only on an explicit user action (hotkey release / 2nd toggle press), never on a pause in speech, so the user can think mid-utterance without being cut off. The earlier energy-gated toggle auto-endpoint was **removed** (`audio::energy_to_prob` + the `dictation://auto-endpoint` wiring) because it cut dictations on natural pauses; the `vad.rs` `EndpointDetector` (pure + cargo-tested) is retained as the primitive for a backlog in-process Silero endpoint path. Server-side `--vad --vad-model <silero>` still gates recognition (anti-hallucination). → [audio-capture.md](specs/audio-capture.md), [speech-to-text.md](specs/speech-to-text.md)
- ✅ **Focused-target + elevated-window (UIPI) detection** — wired in the orchestrator via `win32.rs`; an elevated/UAC target returns the run-as-administrator error and no foreground window falls back to clipboard. On-device UAC validation is owner-gated (below). → [text-injection.md](specs/text-injection.md)
- ⬜ Decide the warm-STT default in practice: confirm **whisper-rs in-process** meets the latency target, otherwise stay on the **whisper-server** MVP default ([ADR-004](specs/architecture.md#adr-004-warm-resident-stt)).
- ✅ **Wire the dictionary bias prompt into warm Whisper** — `build_bias_prompt` output is now fed as the per-utterance `/inference` initial prompt (`stt.rs` `transcribe_chunk`/`inference_fields`, built from the dict snapshot in `dictation.rs`); cargo-tested. → [custom-dictionary.md](specs/custom-dictionary.md), [speech-to-text.md](specs/speech-to-text.md)

### External-asset / hardware-gated (code can't finish these alone)

These are the **only** items left, all blocked on assets, hardware, or secrets the project owner must supply; the code paths around them are ready and clippy-clean, but they can't be finished + validated without:

- ✅ **Signed releases auto-cut on `main`** — secrets set + `release.yml` mirrors Toolzy (auto-bump → tag → publish on every push to `main`); signed installers + `latest.json` ship automatically and the in-app updater is live. No manual `v*` tag needed ([ADR-009](specs/architecture.md#adr-009-distribution--auto-update)).
- ✅ **App icons + branding assets** — the supplied `logo.png` was rasterized into the full icon set via `tauri icon` (Windows `.ico`/`.png`/`Square*`/`StoreLogo` + `.icns`); the tray, window, and NSIS installer all use the bundled default icon, so MIA now ships its real branding. (Source is 512×512; a 1024×1024 master would sharpen the largest targets.)
- ⬜ **Deferred major dependency bumps** — `cpal` 0.15 → 0.17 and `enigo` 0.5 → 0.6 are held back; both touch Windows device/input behavior and need on-device re-testing before upgrading.
- ⬜ **On-device validation of the new paths** — per-app style hits, broad PTT-chord testing, and the elevated-window (UAC) injection error each need a real machine / UAC window to confirm; the code is in place ([per-app-context.md](specs/per-app-context.md), [text-injection.md](specs/text-injection.md)).
- ✅ **NVIDIA CUDA engine**: validated on an RTX 4050 — the downloaded cuBLAS whisper-server boots on the GPU (`ggml_cuda_init: found 1 CUDA device … RTX 4050`, `using CUDA0 backend`, model loaded as `CUDA0`), so warm dictation runs GPU-accelerated.

## Backlog / ideas

- 💡 **Streaming live partials** — show interim transcription as you speak.
- 💡 **GPU keep-warm** sub-second end-to-end latency.
- 💡 **"Hey MIA" wake word** — hands-free activation.
- 💡 **Whisper Mode** — recognition tuned for quiet/whispered speech.
- 💡 **macOS / Linux** — deferred (macOS Accessibility/TCC; Linux Wayland injection). See [ADR-011](specs/architecture.md#adr-011-windows-only-v1).
- 💡 **File-transcription mode** — batch transcribe audio/video files via the old cold-file shape: ffmpeg preprocesses arbitrary media to 16 kHz mono WAV, then the same Whisper model registry/download policy feeds a file transcription pipeline.

## Out of scope (deliberate)

- Any cloud, hosted, or server-side processing — MIA is **fully local**; voice never leaves the machine ([ADR-001](specs/architecture.md#adr-001-native-on-device-privacy-first)).
- Accounts, login, cloud sync, or telemetry.
- Paid tiers / feature gating — MIT, free, open-source ([ADR-010](specs/architecture.md#adr-010-licensing)).
- Mobile (iOS/Android). v1 is **Windows x64 only** ([ADR-011](specs/architecture.md#adr-011-windows-only-v1)).
- **AI Command Mode / Polish (the old Phase 2)** — descoped by product decision (2026-05-29). MIA is a faithful, deterministic dictation tool; no local-LLM text rewriting. The dormant pure helpers have been removed from the engine; the design record is preserved in [docs/specs/ai-commands.md](specs/ai-commands.md) + git history should it ever be reconsidered.
