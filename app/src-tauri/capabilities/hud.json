{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "hud",
  "description": "The floating mic HUD overlay window — listens to engine state events; positioning + click-through are done natively in hud.rs.",
  "windows": ["hud"],
  "permissions": ["core:default"]
}
